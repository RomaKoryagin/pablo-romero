<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        {# SEO Meta Tags #}
        <title>{% block title %}{{ seo.title|default('Hieroglyphics Production') }}{% endblock %}</title>
        <meta name="description" content="{% block meta_description %}{{ seo.metaDescription|default('Агентство полного цикла для вашего бизнеса в интернете') }}{% endblock %}">
        <meta name="keywords" content="{% block meta_keywords %}{{ seo.keywords|default('разработка сайтов, веб-разработка, интернет реклама') }}{% endblock %}">

        {# Favicon #}
        <link rel="icon" type="image/png" href="{{ asset('1763275198.png') }}">

        {# Fonts #}
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

        {# Stylesheets #}
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
        {% endblock %}

        {# Javascripts #}
        {% block javascripts %}
            <!-- Yandex.Metrika counter -->
            <script type="text/javascript">
                (function(m,e,t,r,i,k,a){
                    m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
                    m[i].l=1*new Date();
                    for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
                    k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)
                })(window, document,'script','https://mc.yandex.ru/metrika/tag.js?id=105349451', 'ym');

                ym(105349451, 'init', {ssr:true, webvisor:true, clickmap:true, ecommerce:"dataLayer", accurateTrackBounce:true, trackLinks:true});
            </script>
            <noscript><div><img src="https://mc.yandex.ru/watch/105349451" style="position:absolute; left:-9999px;" alt="" /></div></noscript>
            <!-- /Yandex.Metrika counter -->
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}

        {# Default Scripts #}
        {% block scripts %}
            <script src="{{ asset('assets/js/script.js') }}"></script>
        {% endblock %}
    </body>
</html>
